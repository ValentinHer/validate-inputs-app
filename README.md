# validate-inputs-app

Aplicación construida con React Native + TypeScript para validación de entradas (inputs) en formularios / UI.

## 📄 Descripción

validate-inputs-app es una app que permite validar entradas del usuario en interfaces móviles — solo para formularios sencillos que necesiten validación.  
El objetivo es ofrecer una base ligera con lógica de validación para campos, ideal para usar como plantilla o referencia en otras apps hechas con React Native.

## ⚙️ Requisitos

- Node.js
- npm
- React Native  
- Expo para tu teléfono móvil, esto para testear la aplicación.  

## 🚀 Instalación & Ejecución

# Instalar dependencias
```bash
npm install
``` 

# Ejecutar la app
```bash
npm run start
```
